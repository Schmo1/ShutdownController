<UserControl x:Class="ShutdownController.Views.LegendsViewDisk"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:strings="clr-namespace:ShutdownController.Resources.DiskStrings">


    
    
    <StackPanel Orientation="Horizontal">
        <Border Width="12" Height="12" Background="#709999" CornerRadius="3" />
        <TextBlock Text="{x:Static strings:DiskStrings.read}" VerticalAlignment="Center" Margin="7 0 20 0" Foreground="#8ca7ba" />

        <Border Width="12" Height="12" Background="#faaa66" CornerRadius="3" />
        <TextBlock Text="{x:Static strings:DiskStrings.write}" VerticalAlignment="Center" Margin="7 0 20 0" Foreground="#8ca7ba" />

    </StackPanel>
</UserControl>
